# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9        # Required by FastAPI for form-based login

# ── Authentication ─────────────────────────────────────────────────────────────
passlib[bcrypt]>=1.7.4         # Password hashing
python-jose[cryptography]>=3.3.0  # JWT creation and validation

# ── Database ───────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0              # ORM — supports SQLite (MVP) and PostgreSQL (post-MVP)
# psycopg2-binary>=2.9.0       # Uncomment when migrating to PostgreSQL at v1.1+

# ── Environment Configuration ──────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── XML Parsing (L5X) ─────────────────────────────────────────────────────────
lxml>=5.1.0

# ── Testing ────────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0              # Coverage reporting
